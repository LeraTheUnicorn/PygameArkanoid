# Руководство по релизу игры Арканоид

## 🚀 Готовый исполняемый файл

### Arkanoid_v1.6.1.exe

**Размер:** ~30 МБ  
**Требования:** Windows 7/10/11 (64-bit)  
**Зависимости:** Включены (pygame 2.6+, numpy)

#### Использование:
1. Скачайте файл `Arkanoid_v1.6.1.exe`
2. Запустите двойным кликом
3. Игра готова к использованию без установки Python!

#### Особенности:
- ✅ Полностью переносимый - работает с любого места
- ✅ Включены все звуковые эффекты и музыка
- ✅ Система рекордов работает в папке с игрой
- ✅ Не требует прав администратора

## 🔧 Создание нового релиза

### Автоматический релиз
```bash
python release.py
```

Этот скрипт:
1. Увеличит версию (1.6.1 → 1.6.2)
2. Обновит версии во всех файлах
3. Создаст запись в changelog
4. Соберет новый исполняемый файл
5. Поместит готовый .exe в корневую папку

### Ручная сборка
```bash
# Очистка предыдущих сборок
rm -rf dist build *.spec

# Сборка с PyInstaller
pyinstaller --onefile --windowed --name Arkanoid_v1.6.1 ^
    --add-data "sounds;sounds" ^
    --add-data "images;images" ^
    --hidden-import pygame ^
    --hidden-import numpy ^
    --exclude-module tkinter ^
    --exclude-module matplotlib ^
    --clean PyGameBall.py

# Копирование в корневую папку
copy dist\Arkanoid_v1.6.1.exe .
```

## 📁 Структура проекта

```
PythonProject2/
├── Arkanoid_v1.6.1.exe          # Исполняемый файл (готово к распространению)
├── PyGameBall.py                # Основной код игры
├── highscores.py                # Модуль рекордов
├── sounds/                      # Звуковые файлы
│   └── Night_Prowler.ogg        # Фоновая музыка
├── images/                      # Изображения
│   └── img.png                  # Скриншот игры
├── build_spec.py               # Скрипт сборки PyInstaller
├── release.py                  # Автоматический релиз
├── setup.py                    # Скрипт сборки py2exe (устаревший)
├── README.md                   # Документация проекта
├── README_RELEASE.md           # Этот файл
├── changelog.md                # История изменений
└── pyproject.toml              # Конфигурация Poetry
```

## 🎮 Функции игры

- **Классический геймплей Арканоид** - отбивайте мяч платформой
- **Система рекордов** - топ-10 лучших результатов
- **Звуковые эффекты** - процедурная генерация 16-битных звуков
- **Фоновая музыка** - Night Prowler
- **Полная локализация** - русский язык интерфейса

## 🔍 Технические детали

### Включенные зависимости:
- Python 3.8+ runtime
- Pygame 2.6.1 (игровая графика и звук)
- NumPy (генерация звуковых волн)
- Все системные библиотеки Windows

### Оптимизации:
- Единый исполняемый файл (--onefile)
- Исключены ненужные модули
- Сжатие ресурсов
- Оптимизированный размер ~30 МБ

## 📝 Версионирование

- **Мажорная версия** (X.0.0) - крупные изменения игрового процесса
- **Минорная версия** (X.Y.0) - новые функции, исправления
- **Патч версия** (X.Y.Z) - мелкие исправления, оптимизации

### Автоматическое обновление версий:
- `PyGameBall.py` - VERSION = "X.Y.Z"
- `pyproject.toml` - version = "X.Y.Z" 
- `README.md` - бейдж и текст версии
- `changelog.md` - записи об изменениях

## 🎯 Следующие шаги

1. **Тестирование** - проверьте работу на разных системах
2. **Распространение** - поделитесь готовым .exe файлом
3. **Обратная связь** - соберите отзывы пользователей
4. **Планирование** - определите цели для следующей версии

---

**Готово к распространению! 🎉**